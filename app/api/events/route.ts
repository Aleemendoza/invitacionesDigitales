import { NextRequest, NextResponse } from "next/server";
import { getAdminSupabase } from "@/lib/public-guest-server";
export async function GET(request:NextRequest){const token=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"");if(!token)return NextResponse.json({error:"Iniciá sesión."},{status:401});const db=getAdminSupabase();const {data:auth}=await db.auth.getUser(token);if(!auth.user)return NextResponse.json({error:"Sesión vencida."},{status:401});const {data,error}=await db.from("events").select("id,slug,title,event_type,starts_at,status,template_slug,plan,payment_status,content").eq("owner_id",auth.user.id).order("created_at",{ascending:false});return error?NextResponse.json({error:"No pudimos cargar tus eventos."},{status:500}):NextResponse.json({events:data??[]});}
