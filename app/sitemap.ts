import type { MetadataRoute } from "next";
import { templates } from "@/lib/templates";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.APP_URL?.startsWith("http")?process.env.APP_URL:"https://papeleta.app";const now=new Date();const paths=["","/plantillas","/precios","/la-armamos-por-vos","/privacidad","/terminos","/invitaciones/bodas","/invitaciones/xv","/invitaciones/cumpleanos","/invitaciones/infantiles","/invitaciones/corporativos"];return[...paths.map((path)=>({url:`${base}${path}`,lastModified:now,changeFrequency:path===""?"weekly" as const:"monthly" as const,priority:path===""?1:.8})),...templates.map(({slug})=>({url:`${base}/plantillas/${slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.7}))]}
