import { AnalyticsLink } from "@/components/analytics-link";
import { SiteFooter, SiteHeader } from "@/components/site-shell";
import { planDetails, plans } from "@/lib/event-drafts";

export function PricingPage() {
  return <main><SiteHeader/><section className="section pricing"><p className="eyebrow">PLANES Y PRECIOS</p><h1>Elegí lo que necesita<br/><em>tu celebración.</em></h1><p className="lead">Creá el borrador sin costo. Cuando esté listo, pagá de forma segura con Mercado Pago para publicarlo.</p><div className="plans">{plans.map((plan,index)=>{const detail=planDetails[plan];return <article className={`planCard plan${index === 0 ? "Essential" : index === 1 ? "Plus" : "Premium"}`} key={plan}>{plan === "premium"&&<span className="planBadge">Más elegido</span>}<div className="planCopy"><p className="eyebrow">PLAN {detail.name.toUpperCase()}</p><h2>{detail.name}</h2><strong className="planPrice">${detail.price.toLocaleString("es-AR")}</strong><p>{detail.description}</p><p className="planIncludes">Incluye</p><ul aria-label={`Incluye plan ${detail.name}`}>{detail.marketingFeatures.map((feature)=><li key={feature}><span aria-hidden="true">✓</span>{feature}</li>)}</ul><div className="planActions"><AnalyticsLink className="button dark" href={`/crear?plan=${plan}`} analytics={{ name:"create_start", plan }}>Crear con {detail.name}</AnalyticsLink><small>El precio se valida al iniciar el pago.</small></div></div></article>})}</div><p className="pricingHelp">¿No querés configurarla? <a href="/la-armamos-por-vos">Conocé el servicio “La armamos por vos”.</a></p></section><SiteFooter/></main>;
}
