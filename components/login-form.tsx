"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { getBrowserSupabase } from "@/lib/supabase-browser";
export function LoginForm() { const router = useRouter(); const searchParams=useSearchParams(); const [mode,setMode]=useState<"login"|"signup">("login"); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); const [saving,setSaving]=useState(false); const submit=async(event:FormEvent)=>{event.preventDefault();setSaving(true);setMessage("");try{const client=getBrowserSupabase();if(!client)throw new Error("No encontramos la configuración de acceso.");const result=mode==="login"?await client.auth.signInWithPassword({email,password}):await client.auth.signUp({email,password});if(result.error)throw result.error;if(mode==="signup"&&!result.data.session){setMessage("Revisá tu email para confirmar la cuenta.");return}const next=searchParams.get("next");router.push(next?.startsWith("/")?next:"/mis-eventos");router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"No pudimos iniciar sesión.")}finally{setSaving(false)}};return <main className="auth"><section><Link className="brand" href="/">celebra<span>✦</span></Link><p className="eyebrow">{mode==="login"?"VOLVÉ A TU CELEBRACIÓN.":"EMPEZÁ A CREAR."}</p><h1>{mode==="login"?"Qué lindo tenerte de vuelta.":"Tu próxima celebración empieza acá."}</h1><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={event=>setEmail(event.currentTarget.value)}/></label><label>Contraseña<input type="password" required minLength={6} value={password} onChange={event=>setPassword(event.currentTarget.value)}/></label>{message&&<p className="formError" role="alert">{message}</p>}<button className="button dark" disabled={saving}>{saving?"Ingresando…":mode==="login"?"Ingresar":"Crear cuenta"}</button></form><button className="textButton" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Crear una cuenta":"Ya tengo cuenta"}</button></section><div/></main> }
