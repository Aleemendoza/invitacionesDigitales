"use client";
import {FormEvent,useState} from "react";
import Link from "next/link";
import {useRouter,useSearchParams} from "next/navigation";
import {getBrowserSupabase} from "@/lib/supabase-browser";
export function LoginForm(){const router=useRouter(),params=useSearchParams(),[mode,setMode]=useState<"login"|"signup">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[saving,setSaving]=useState(false);const submit=async(event:FormEvent)=>{event.preventDefault();setSaving(true);setMessage("");try{const client=getBrowserSupabase();if(!client)throw new Error("No encontramos la configuración de acceso.");const result=mode==="login"?await client.auth.signInWithPassword({email,password}):await client.auth.signUp({email,password});if(result.error)throw result.error;if(mode==="signup"&&!result.data.session){setMessage("Revisá tu email para confirmar la cuenta.");return}const {data:{session}}=await client.auth.getSession();if(!session)throw new Error("No pudimos conservar tu sesión. Volvé a intentarlo.");const next=params.get("next");router.replace(next?.startsWith("/")?next:"/mis-eventos");router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"No pudimos iniciar sesión.")}finally{setSaving(false)}};return <main className="auth"><section><Link className="brand" href="/">celebra<span>✦</span></Link><p className="eyebrow">{mode==="login"?"VOLVÉ A TU CELEBRACIÓN.":"EMPEZÁ A CREAR."}</p><h1>{mode==="login"?"Qué lindo tenerte de vuelta.":"Tu próxima celebración empieza acá."}</h1><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.currentTarget.value)}/></label><label>Contraseña<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.currentTarget.value)}/></label>{message&&<p className="formError" role="alert">{message}</p>}<button className="button dark" disabled={saving}>{saving?"Ingresando…":mode==="login"?"Ingresar":"Crear cuenta"}</button></form><button className="textButton" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Crear una cuenta":"Ya tengo cuenta"}</button></section><div/></main>}
